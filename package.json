{
  "name": "glancer-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "http-server",
    "deploy": "ftpsync -v"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ftpsync": "^0.1.10",
    "http-server": "^0.9.0"
  }
}
